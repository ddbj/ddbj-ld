import { requestPost, requestGet, requestDelete } from "./common"
import config from "../config"

// DDBJアカウント（OpenAM／OpenDJ）に対するAPI

const getInvitationList = (projectId) => {

}

export {
    getInvitationList
}